package collectionsInfo;

public class setInfo {
    /* The Set interface is identical to the Collection interface,
       but the behavior of the methods is more tightly deﬁned.
       Making a Set interface enables programmers to write methods that accept only sets.
       E set(int i, E element) replaces the element at the speciﬁed position with a new element and returns the old element.
 */

}
