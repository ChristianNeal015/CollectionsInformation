package collectionsInfo;

public class iteratorInfo {
    // The List interface deﬁnes several methods for random access:

    // void add(int index, E element)
    // void remove(int index) E get(int index)
    // E set(int index, E element)

    // The ListIterator interface is a subinterface of Iterator.
    // It deﬁnes a method for adding an element before the iterator position:
}
